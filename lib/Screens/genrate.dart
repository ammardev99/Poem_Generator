import 'package:flutter/material.dart';
class GenratePoem extends StatelessWidget {
  const GenratePoem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Genrate Poem"),
      ),
    );
  }
}